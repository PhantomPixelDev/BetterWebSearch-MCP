## Summary

<!-- Briefly describe what this PR does and why. -->

## Changes

<!-- Bullet-point list of what changed. -->

-

## Checklist

- [ ] `npm run lint` passes with zero errors (`tsc --noEmit`)
- [ ] `npm test` passes (`vitest run` — all 189+ tests green)
- [ ] `npm run build` succeeds (`dist/index.js` emitted correctly)
- [ ] `CHANGELOG.md` updated (if user-facing change)
- [ ] `npm pack --dry-run` reviewed — tarball contains expected files only
- [ ] No secrets, API keys, or credentials committed (reviewed `git diff`)

## Breaking changes

<!-- List any breaking changes or write "None". -->

## Additional notes

<!-- Optional: testing instructions, follow-up work, screenshots, related issues. -->
